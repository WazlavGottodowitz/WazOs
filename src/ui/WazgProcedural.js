window.WazgProcedural = {
  init: function() {
    if (window.WazgLogcat) window.WazgLogcat.log("PROC", "Procedural Animation Engine loaded");
  },

  addBreathing: function(rig) {
    if (window.WazgLogcat) window.WazgLogcat.log("PROC", "Breathing animation added");
  }
};
