window.WazgDeCasteljauVisualizer = {
  init: function() {
    if (window.WazgLogcat) window.WazgLogcat.log("DECASTELJAU", "De Casteljau Visualizer ready");
  },

  visualize: function() {
    if (window.WazgLogcat) window.WazgLogcat.log("DECASTELJAU", "Showing construction steps");
  }
};
